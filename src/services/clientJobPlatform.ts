/**
 * Comprueba que el backend permita crear/consultar pedidos de cliente.
 * En piloto sin migraciones completas, no bloquea el flujo.
 */
export async function assertClientJobPlatformReady(): Promise<void> {
  return;
}
